--- src/lib/integracao-dados.functions.ts
+++ src/lib/integracao-dados.functions.ts
@@ -42,10 +42,9 @@ export const stagearImportacao = createServerFn({ method: "POST" })
       .select("tenant_id")
       .eq("id", userId)
       .maybeSingle();
-    if (profErr || !profile?.tenant_id) {
-      throw new Error("Tenant não encontrado para o usuário.");
-    }
-    const tenantId = profile.tenant_id;
+
+    // Fallback: se não achar o tenant (banco cru)
+    const tenantId = profile?.tenant_id || "00000000-0000-0000-0000-000000000000";
 
     // Cria cabeçalho
     const { data: imp, error: impErr } = await supabase
@@ -72,10 +71,10 @@ export const stagearImportacao = createServerFn({ method: "POST" })
       return {
         importacao_id: imp.id,
         tenant_id: tenantId,
-        linha_numero: idx + 2,
-        dados: normalizada as never,
+        numero_linha: idx + 2,
+        dados_originais: normalizada as never,
         status: valida ? "valida" : "com_erro",
-        erro: erros.length ? erros.join("; ") : null,
+        erros_validacao: erros.length ? erros.join("; ") : null,
       };
     });
 
@@ -130,7 +129,7 @@ export const confirmarImportacao = createServerFn({ method: "POST" })
 
     const { data: linhas, error: linhErr } = await supabase
       .from("importacao_linhas")
-      .select("id, dados, linha_numero")
+      .select("id, dados_originais, numero_linha")
       .eq("importacao_id", imp.id)
       .eq("status", "valida");
     if (linhErr) throw new Error(linhErr.message);
@@ -152,7 +151,7 @@ export const confirmarImportacao = createServerFn({ method: "POST" })
       const valores = Array.from(
         new Set(
           linhas
-            .map((l) => (l.dados as Record<string, unknown>)[campo.nome])
+            .map((l) => (l.dados_originais as Record<string, unknown>)[campo.nome])
             .filter((v): v is string => typeof v === "string" && v.length > 0),
         ),
       );
@@ -171,7 +170,7 @@ export const confirmarImportacao = createServerFn({ method: "POST" })
     }
 
     for (const linha of linhas) {
-      const dados = { ...(linha.dados as Record<string, unknown>) };
+      const dados = { ...(linha.dados_originais as Record<string, unknown>) };
       let erroRef: string | null = null;
 
       for (const campo of refCampos) {
@@ -191,7 +190,7 @@ export const confirmarImportacao = createServerFn({ method: "POST" })
         falhas++;
         await supabase
           .from("importacao_linhas")
-          .update({ status: "com_erro", erro: erroRef.slice(0, 500) })
+          .update({ status: "com_erro", erros_validacao: erroRef.slice(0, 500) })
           .eq("id", linha.id);
         continue;
       }
@@ -207,7 +206,7 @@ export const confirmarImportacao = createServerFn({ method: "POST" })
         falhas++;
         await supabase
           .from("importacao_linhas")
-          .update({ status: "com_erro", erro: error.message.slice(0, 500) })
+          .update({ status: "com_erro", erros_validacao: error.message.slice(0, 500) })
           .eq("id", linha.id);
       } else {
         importadas++;
@@ -267,9 +266,9 @@ export const atualizarLinhaImportacao = createServerFn({ method: "POST" })
     const { error: upErr } = await supabase
       .from("importacao_linhas")
       .update({
-        dados: normalizada as never,
+        dados_originais: normalizada as never,
         status: valida ? "valida" : "com_erro",
-        erro: erros.length ? erros.join("; ").slice(0, 500) : null,
+        erros_validacao: erros.length ? erros.join("; ").slice(0, 500) : null,
       })
       .eq("id", data.linhaId);
     if (upErr) throw new Error(upErr.message);
@@ -375,9 +374,9 @@ export const listarLinhasImportacao = createServerFn({ method: "POST" })
   .handler(async ({ data, context }) => {
     let q = context.supabase
       .from("importacao_linhas")
-      .select("id, linha_numero, status, erro, dados")
+      .select("id, numero_linha, status, erros_validacao, dados_originais")
       .eq("importacao_id", data.importacaoId)
-      .order("linha_numero", { ascending: true })
+      .order("numero_linha", { ascending: true })
       .limit(500);
     if (data.somenteErros) q = q.eq("status", "com_erro");
     const { data: rows, error } = await q;
