--- src/services/industrial-mes.service.ts
+++ src/services/industrial-mes.service.ts
@@ -82,7 +82,7 @@
       });
 
       // Atualiza a ordem de produção real com tipagem forçada (SafeAny)
-      const updatePayload = {
+      const updatePayload: SafeAny = {
         status: newStatus,
         quantity_produced: data.quantity_produced + quantity,
       };
@@ -131,7 +131,7 @@
       });
 
       // Atualiza quantidade de refugo e data final com tipagem forçada
-      const updatePayload = {
+      const updatePayload: SafeAny = {
         status: 'Completed',
         quantity_scrap: data.quantity_scrap + scrapQuantity,
         end_time: new Date().toISOString()
--- src/services/qms-capa.service.ts
+++ src/services/qms-capa.service.ts
@@ -87,7 +87,7 @@
     
     if (actionType === 'Correção') {
       // Se for correção imediata, já fecha a NC e vai para verificação
-      const updatePayload = {
+      const updatePayload: SafeAny = {
         status: 'Verificação'
       };
       await supabase
@@ -131,7 +131,7 @@
       throw new Error('Falha ao registrar ação CAPA. ' + error.message);
     }
 
-    const updatePayload = {
+    const updatePayload: SafeAny = {
       status: isLastAction ? 'Verificação' : 'Em Andamento'
     };
 
@@ -189,7 +189,7 @@
     }
 
     // Atualiza a NC para fechada
-    const updatePayload = {
+    const updatePayload: SafeAny = {
       status: 'Fechada',
       closed_at: new Date().toISOString()
     };
--- src/services/qms-document.service.ts
+++ src/services/qms-document.service.ts
@@ -89,7 +89,7 @@
       throw new Error('Falha ao criar o documento. ' + error.message);
     }
 
-    const auditPayload = {
+    const auditPayload: SafeAny = {
       tenant_id: tenantId,
       table_name: 'documents',
       record_id: data.id,
@@ -133,7 +133,7 @@
       throw new Error('Falha ao revisar o documento. ' + error.message);
     }
 
-    const auditPayload = {
+    const auditPayload: SafeAny = {
       tenant_id: tenantId,
       table_name: 'documents',
       record_id: documentId,
